<% content_for :body_content_title do %>
  <%= @job_sub_category.title %>
<% end %>

<% content_for :body_content_left do %>
  
  <div>
    <h3>Title</h3>
    <%= @job_sub_category.title %>
  </div>
  
  <div>
    <h3>Job Category</h3>
    <%= @job_sub_category.job_category %>
  </div>
  
  <div>
    <h3>Description</h3>
    <%= @job_sub_category.description %>
  </div>
  
<% end %>

<% content_for :body_content_right do %>
  <h2><%= t('.other') %></h2>
  <ul id="job_sub_categories">
    <% @job_sub_categories.each do |job_sub_category| %>
      <li>
        <%= link_to job_sub_category.title, job_sub_category_url(job_sub_category) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render :partial => "/shared/content_page" %>
